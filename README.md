# CreativeGNN
Creative-GNN is a PyTorch reference implementation of a physics-embedded Graph Neural ODE for aerodynamic flow prediction. It models meshes as graphs, evolves states with continuous-depth message passing, adds region-aware local solvers, and enforces conservation via a flux layer plus physics-informed losses. Configs and scripts enable repeat runs.
